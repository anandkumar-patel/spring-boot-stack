server:
  port: 9296

spring:
  application:
    name: cloud-config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/anandkumar-patel/config-server
          clone-on-start: true
